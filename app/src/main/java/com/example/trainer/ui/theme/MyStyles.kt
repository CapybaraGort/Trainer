package com.example.trainer.ui.theme

import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.unit.sp

object MyStyles {
    val textH1 = TextStyle(
        fontSize = 24.sp,
        fontFamily = displayFontFamily,
    )
    val textP = TextStyle(
        fontSize = 16.sp,
        fontFamily = bodyFontFamily
    )
}